// const xyz = require('./people');

const { name, num } = require('./people');

// console.log(xyz.name, xyz.num);

console.log(name, num);

const os = require('os');

console.log(os.platform(), os.homedir());